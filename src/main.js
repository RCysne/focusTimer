import './toggle-view.js';


// Importou tudo para um objeto chamado FocusTimer (pode ser qualquer nome)
import * as FocusTimer from  './FocusTimer/index.js'

// Usando o objeto importado e chamando a função passando os parâmetros
FocusTimer.start(0, 10)

console.log(FocusTimer)